// TODO: convert the whole application to an angular application
var UI = angular.module('UIMain',[])

.factory('filter',[function () {}])

.config([function () {}])

.run([function () {
   handlers.navigationListener();
}]);
